<source>
  @type tail
  path /fluentd/log/app.log
  pos_file /fluentd/log/app.pos
  tag spring.json
  format json
</source>

<match spring.json>
  @type elasticsearch
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix spring-log
  flush_interval 5s
</match>
